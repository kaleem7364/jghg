<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>موقع تجريبي - فتح بواسطة المفتاح</title>
    <script src="https://cdn.jsdelivr.net/npm/typed.js@2.0.12"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.11.4/gsap.min.js"></script>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            background: #f0f0f0;
            overflow: hidden;
        }
        
        .intro-screen {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            background: #1a1a2e;
            color: white;
            z-index: 1000;
        }
        
        .logo {
            font-size: 2.5rem;
            margin-bottom: 2rem;
            height: 50px;
        }
        
        .key-door-animation {
            display: flex;
            align-items: center;
            gap: 20px;
        }
        
        .key {
            width: 80px;
            transition: all 0.5s;
        }
        
        .door {
            width: 100px;
            opacity: 0.8;
        }
        
        .website-content {
            display: none;
            padding: 2rem;
            text-align: center;
        }
        
        .skip-intro {
            position: absolute;
            top: 20px;
            right: 20px;
            background: rgba(255, 255, 255, 0.2);
            border: none;
            color: white;
            padding: 5px 10px;
            border-radius: 5px;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <div class="intro-screen">
        <button class="skip-intro">تخطي</button>
        <div class="logo" id="typed-logo"></div>
        <div class="key-door-animation">
            <img src="https://cdn-icons-png.flaticon.com/512/1828/1828919.png" class="key" alt="Key">
            <img src="https://cdn-icons-png.flaticon.com/512/2750/2750761.png" class="door" alt="Door">
        </div>
    </div>
    
    <main class="website-content">
        <h1>مرحباً بك في الموقع!</h1>
        <p>لقد تم فتح الموقع بواسطة المفتاح بنجاح. 🎉</p>
    </main>

    <script>
        // Typewriter Effect for Logo
        const typed = new Typed('#typed-logo', {
            strings: ["اسم العلامة التجارية"],
            typeSpeed: 100,
            showCursor: false,
            onComplete: startKeyAnimation
        });

        // Key Unlock Animation
        function startKeyAnimation() {
            gsap.to(".key", {
                x: 40,
                duration: 1.5,
                ease: "power2.out",
                onComplete: () => {
                    // Door shake effect
                    gsap.to(".door", { 
                        scale: 1.2, 
                        duration: 0.2, 
                        yoyo: true, 
                        repeat: 1,
                        onComplete: showWebsite 
                    });
                }
            });
        }

        // Show Main Website
        function showWebsite() {
            gsap.to(".intro-screen", {
                opacity: 0,
                duration: 0.8,
                onComplete: () => {
                    document.querySelector(".intro-screen").style.display = "none";
                    document.querySelector(".website-content").style.display = "block";
                }
            });
        }

        // Skip Intro Button
        document.querySelector(".skip-intro").addEventListener("click", showWebsite);
    </script>
</body>
</html>
